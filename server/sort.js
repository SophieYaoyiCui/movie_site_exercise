/* sort the movies by imdbRating and release date
 * normally people build a basic comparison function, 
 * I wonder if I can use storage of previous value and create an index

 */


for (var i = 0; i < list.length; i++ ){
	var rating = list[i].body.imdbRating;
	var releaseDate = list[i].body.Released;
	if(rating && releaseDate){

	}
}